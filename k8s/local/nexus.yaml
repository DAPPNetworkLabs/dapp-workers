apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f test_compose.yaml -o ./k8s/test/
    kompose.version: 1.26.0 (40646f47)
  creationTimestamp: null
  labels:
    app: nexus
    product: dapp-workers
  name: nexus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexus
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f test_compose.yaml -o ./k8s/test/
        kompose.version: 1.26.0 (40646f47)
      creationTimestamp: null
      labels:
        app: nexus
        product: dapp-workers
    spec:
      containers:
        - args:
            - /bin/bash
            - /app/nexus/wait-for-it.sh
            - --timeout=15
            - eth:8545
            - --
            - npx
            - hardhat
            - --network
            - tests
            - test
            - test/Nexus.spec.js
          env:
            - name: ETH_ADDR
              value: ws://eth:8545
            - name: PRIVATE_KEY
              value: 0x278c2ff8b0fa8bbe04c430a66c828f8b2386a0e9c075b8923d257c3be30c697d
            - name: SEED
              value: cruel rebel frown short month love belt weather sense hood cage pact
            - name: RUN_LOCAL
              value: "true"
            - name: ONLY_CONTRACTS
              value: "true"
          image: natpdev/nexus
          name: nexus
          resources: {}
          volumeMounts:
            - mountPath: /services/orchestrator
              name: nexus-claim0
              readOnly: true
      volumes:
        - name: nexus-claim0
          hostPath:
            # path: /dapp-workers/services/orchestrator
            path: /k8s/home/ubuntu/dapp-workers/services/orchestrator
            # type: Directory
status: {}
