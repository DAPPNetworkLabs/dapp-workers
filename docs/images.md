wasm-runner - pulls WASM from IPFS runs and returns output
rust-compiler - compiles rust file to WASM pins to IPFS
wasi service - runs WASM as API

history-fetcher
ipfs manipulator/merger
git cloner
solidity runner